apiVersion: v1
kind: Service
metadata:
  name: test-service
  namespace: metallb-system
spec:
  type: LoadBalancer
  selector:
    app: test
  ports:
    - port: 80
      targetPort: 80
---
apiVersion: v1
kind: Pod
metadata:
  name: test-pod
  namespace: metallb-system
  labels:
    app: test
spec:
  containers:
    - name: nginx
      image: nginx:latest
      ports:
        - containerPort: 80
